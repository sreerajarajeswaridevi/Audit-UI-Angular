import { ToFixedDirective } from './to-fixed.directive';

describe('ToFixedDirective', () => {
  it('should create an instance', () => {
    const directive = new ToFixedDirective();
    expect(directive).toBeTruthy();
  });
});

import { DateTransformDirective } from './date-transform.directive';

describe('DateTransformDirective', () => {
  it('should create an instance', () => {
    // const directive = new DateTransformDirective();
    // expect(directive).toBeTruthy();
  });
});
